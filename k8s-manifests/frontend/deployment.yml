apiVersion: apps/v1
kind: Deployment
metadata:
  name: fronted 
  namespace: banking

spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      nodeSelector:
        kubernetes.io/hostname: workernode1
      containers:
        - name: frontend
          image: hariomharish/java-fintech-frontend:v1
    
            - containerPort: 80
          